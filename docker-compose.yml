version: '3'
services:
  web:
    build: .
    ports:
     - "5000:5000"
    volumes:
     - ${DEBUGMOUNT}
     - honda_box:/data/boxes
    tmpfs:
     - /tmp
  redis:
    image: "redis:alpine"
    volumes:
     - redis_data:/data
volumes:
  honda_box:
  redis_data:
